# Geparkeerd werk

Deze map wordt door Jekyll niet gepubliceerd (`exclude` in `_config.yml`).

- `training/` — *Van Drukte naar Regie*: overzichtspagina, drie basispagina's en de CTA-include, gebouwd op 23 augustus 2026 en dezelfde dag geparkeerd. Mark wil dit op termijn inzetten om Notion-sjablonen te verkopen (geen trainingen), maar nu nog niet. Terugzetten: bestanden naar `_pages/` en `_includes/`, `sjablonen_url` in `_config.yml`, menu-item en homepage-sectie (zie git-historie van 23 aug 2026). De CSS (`TRAININGEN`-blok in `main.scss`) staat er nog.
